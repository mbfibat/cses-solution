#include <bits/stdc++.h>

using namespace std;

int n, m;

string s;

int main(int argc, char** argv)
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	cin >> n >> m;
	cin >> s;
	s = '*' + s;
	while (k--)
	{
		int type;
		cin >> type;
		if (type == 1)
		{
			int k, x;
			cin >> k >> x;
			s[k] = x;
		}
		else
		{
		}
	}
}